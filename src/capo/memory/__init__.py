"""capo.memory — Episodic memory across CAPO fine-tuning runs.

Provides the `runs_index.md` registry and `RUN_REPORT.md` helpers used by the
finalizer (to write reports + append to the index) and by the memory-consultant
subagent (to scan past frontmatters and selectively load full reports).
"""
