"""MCP tool implementations.

Each module in this package exposes pure-Python tool functions (no FastMCP
coupling). The MCP server files under :mod:`capo.mcp.server` register them
with FastMCP and add the JSON-string envelope and trace logging.
"""
