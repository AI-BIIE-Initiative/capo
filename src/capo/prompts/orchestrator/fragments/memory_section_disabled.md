## Prior-run memory (episodic) — DISABLED
Episodic memory was disabled for this run (enable_memory=false). The cross-run
index `<repo>/runs/runs_index.md` was NOT consulted, no memory-consultant ran,
and there is NO `prior_runs.md`. Do not look for it. Make every decision purely
from current-run artifacts (task.md, profile.json, model_selection.json, probe
results). This run still produces and registers its own RUN_REPORT.md normally
at the finalize phase — only the reading of past runs is turned off.